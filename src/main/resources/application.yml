spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/yomu
    username: yomu
    password: yomu

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

  flyway:
    enabled: false

server:
  port: 8080

app:
  jwt:
    secret: ${JWT_SECRET:dev-secret-change-this}
    expiration-minutes: 120