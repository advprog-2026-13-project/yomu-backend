spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/yomu
    username: yomu
    password: yomu

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

  flyway:
    enabled: false

server:
  port: 8080

logging:
  level:
    org.springframework.boot.context.config: DEBUG