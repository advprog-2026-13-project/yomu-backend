services:
  db:
    image: postgres:16
    container_name: yomu-postgres
    environment:
      POSTGRES_DB: yomu
      POSTGRES_USER: yomu
      POSTGRES_PASSWORD: yomu
    ports:
      - "5432:5432"
    volumes:
      - yomu_pgdata:/var/lib/postgresql/data

volumes:
  yomu_pgdata:
